/**
 * people who are allowed to do everything
 */
export const ROOTADMIN = [
    "122814281308700673", // Laxis
];

export const ID_REGEX = /^[\da-f]{8}-[\da-f]{4}-[\da-f]{4}-[\da-f]{4}-[\da-f]{12}$/;
